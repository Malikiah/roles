
{
  "server": true,
  "node_name": "{{ ansible_hostname}}",
  "datacenter": "{{ region }}",
  "data_dir": "/var/consul/data",
  "bind_addr": "0.0.0.0",
  "client_addr": "0.0.0.0",
  "advertise_addr": "{{ ansible_default_ipv4.address }}",
  "bootstrap_expect": {{ groups['servers']|length }},
  
  "retry_join": {{ groups['servers'] }},
  
  "ui": true,
  "log_level": "DEBUG",
  "enable_syslog": true,
  "acl_enforce_version_8": false
}
